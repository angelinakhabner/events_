export function MyPage() {
  return (
    <section>
      <h2>My folders</h2>
      <p>Folders let you group venues with persistent filters. (Coming next — auth + folders backend.)</p>
    </section>
  );
}
