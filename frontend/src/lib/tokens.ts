/**
 * The poster palette as plain values.
 *
 * Tailwind owns these colours for classes (see tailwind.config.js); this module
 * exists for the handful of places that need them at runtime — the category
 * swatches are drawn with `clip-path`/`border-radius` and so have to be inline
 * styles rather than utility classes.
 */
export const INK = '#141414';
export const PAPER = '#eae6df';
export const PANEL = '#f4f1ea';
export const ACCENT = '#c62828';
export const MUTED = '#6b6660';
export const FAINT = '#8a8378';
export const RULE = '#cfc9bf';
